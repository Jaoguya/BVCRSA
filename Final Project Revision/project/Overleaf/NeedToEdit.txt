================================================================================
  OVERLEAF SYNC LIST -- 2026-08-19, apply these to Overleaf.com yourself
  ================================================================================
  RULE FROM HERE ON: Claude does not edit Overleaf/BVCRSA directly anymore.
  This section is the only channel for manuscript changes. Everything below
  is built from a line-level `git diff` between the real AWS-data fixes and
  the current file (which mirrors your live Overleaf.com project), not from
  memory. Line numbers are from the CURRENT Overleaf/BVCRSA (= current
  Overleaf.com content) as of this write.

  CONFIRMED ALREADY CORRECT ON OVERLEAF (screenshots checked directly,
  no action needed): Table V's numbers (only the "Estimated" label below
  is wrong), Table VI (all real, matches exactly), Table VIII (all real,
  matches exactly).

--------------------------------------------------------------------------------
1. Communication Cost intro sentence -- ~line 2603
--------------------------------------------------------------------------------
Old overleaf context:
  Table~\ref{tab:communication_cost} compares the query communication
  cost of representative schemes and reports estimated BVCRSA message
  sizes for representative workloads.

New overleaf context:
  Table~\ref{tab:communication_cost} compares the query communication
  cost of representative schemes and reports measured BVCRSA message
  sizes for representative workloads, obtained from the actual
  serialized protocol objects produced by the prototype.

===================

--------------------------------------------------------------------------------
2. Table V caption -- ~line 2607
--------------------------------------------------------------------------------
Old overleaf context:
  \caption{Communication Cost Comparison and Estimated BVCRSA Overhead}

New overleaf context:
  \caption{Communication Cost Comparison and Measured BVCRSA Overhead}

===================

--------------------------------------------------------------------------------
3. Table V section header (inside the table, above the 4-row KB block) -- ~line 2675
--------------------------------------------------------------------------------
Old overleaf context:
  \textbf{Estimated BVCRSA Communication (KB)}

New overleaf context:
  \textbf{Measured BVCRSA Communication (KB)}

===================

--------------------------------------------------------------------------------
4. Table V footnote -- ~line 2705-2706
--------------------------------------------------------------------------------
Old overleaf context:
  BVCRSA values are estimated from the stated protocol-object sizes;
  $r=|\mathcal S_Q|$.}

New overleaf context:
  BVCRSA values are measured from the serialized protocol objects
  produced by the prototype; $r=|\mathcal S_Q|$.}

===================

--------------------------------------------------------------------------------
5. Experimental Setup -- pairing library + hardware description -- ~line 2797-2811
   THIS IS THE MOST IMPORTANT FIX. Answers R2-C3 (implausibly-fast
   sub-ms skepticism) and R1-C5/R3-15 (consistent-hardware complaint).
--------------------------------------------------------------------------------
Old overleaf context:
  A BVCRSA prototype was implemented in Python~3.12 using
  \texttt{py\_ecc} over BN128 for attribute-based searchable
  encryption, NIST P-256 for Threshold EC-ElGamal,
  \texttt{PyCryptodome} for AES-GCM, and SHA-256/HMAC.
  Blockchain-assisted verification used Solidity and
  \texttt{Web3.py} on a private Ethereum Clique-PoA network with
  a 2-s block interval and one-block finality.

  The testbed comprised a Raspberry Pi~4 Model~B (4~GB RAM) for
  sensor operations, an Intel Core~i7 laptop (16~GB RAM) for
  trapdoor generation, and an AWS cloud instance with 32~GB RAM
  and Ubuntu~22.04~LTS for indexing, query processing, aggregation,
  and verification. Synthetic IIoT datasets contained 20 sensor
  categories with values uniformly distributed over $[0,100]$.

New overleaf context:
  A BVCRSA prototype was implemented in Python~3.12 using the
  BLS12-381 bilinear curve via the Rust-native
  \texttt{py\_arkworks\_bls12381} library (128-bit security) for
  attribute-based searchable encryption, with a pure-Python
  \texttt{py\_ecc} BN128 implementation retained as a fallback; all
  reported measurements use the BLS12-381 backend. Threshold
  EC-ElGamal used NIST P-256, \texttt{PyCryptodome} provided AES-GCM,
  and SHA-256/HMAC provided authentication. Blockchain-assisted
  verification used Solidity and \texttt{Web3.py} on a live 3-node
  Ethereum Clique-PoA consortium with a 2-s block interval.

  All experiments were conducted on a single Amazon EC2
  \texttt{r7i.2xlarge} instance (8~vCPUs, 64~GB RAM, Ubuntu~24.04~LTS),
  so that every reported measurement is taken under an identical
  hardware and software configuration; every result CSV records host,
  platform, Python version, and git revision. Synthetic IIoT datasets
  contained 20 sensor categories with values uniformly distributed
  over $[0,100]$.

  WHY: real backend is BLS12-381 (confirmed by every real CSV and
  Exp10's own backend printout), not py_ecc/BN128. Real hardware is
  a single AWS r7i.2xlarge, not the old Pi+laptop+Xeon split.

===================

--------------------------------------------------------------------------------
6. Experimental Setup -- blockchain/sensor-side sentence -- ~line 2820-2828
--------------------------------------------------------------------------------
Old overleaf context:
  recovery are excluded and evaluated separately where applicable.
  Blockchain overhead is assessed through transaction gas, payload size,
  and confirmation latency. Sensor-side experiments on the Raspberry
  Pi~4 evaluate the cryptographic operations specified in
  Table~\ref{tab:sensor_overhead}, including ABSE key encapsulation,
  Threshold EC-ElGamal encryption, AES-GCM encryption, and HMAC
  authentication. For each operation, computation latency and serialized
  output size are measured over 20 independent runs. Peak memory and
  energy consumption are not evaluated.

New overleaf context:
  recovery are excluded from this metric and evaluated separately where
  applicable. Blockchain overhead is measured through transaction gas,
  payload size, and confirmation latency. Sensor-side experiments
  measure cryptographic latency, peak memory, and serialized output
  size on the Raspberry Pi.

===================

--------------------------------------------------------------------------------
7. Baseline Configuration paragraph -- add Latt-IBEKS/VC-KASE/Trinity
   disclosure -- ~line 2840-2846 (end of the paragraph)
   WHY: several fixes below (throughput, query-processing) explain
   Latt-IBEKS's speed by pointing back to this paragraph. Without this
   addition those explanations have nothing to point to.
--------------------------------------------------------------------------------
Old overleaf context:
  with adaptations only for the common testbed. All schemes used the
  same datasets, workloads, hardware, and software, with approximately
  comparable security levels where supported. Comparisons include only
  functionality provided by each baseline; unsupported capabilities
  were not emulated. Equivalent protocol stages were measured under
  identical conditions, excluding scheme-specific preprocessing,
  communication, and blockchain costs unless stated otherwise.

New overleaf context:
  with adaptations only for the common testbed. All schemes used the
  same datasets, workloads, hardware, and software. Comparisons include
  only functionality provided by each baseline; unsupported capabilities
  were not emulated. Equivalent protocol stages were measured under
  identical conditions, excluding scheme-specific preprocessing,
  communication, and blockchain costs unless stated otherwise. Exact
  parameter parity was not achievable for every baseline, and we
  disclose the resulting biases explicitly rather than claim uniform
  security levels. Latt-IBEKS is instantiated with lattice dimension
  $n=17$ and modulus $q=4093$, smaller than a deployment-grade LWE
  parameterization, which \emph{understates} its measured cost relative
  to a production instantiation. VC-KASE is implemented
  with a prime-order group instantiated by modular exponentiation rather
  than a bilinear pairing for its core matching operation, while its
  verification cost is measured using real BLS12-381 pairings. Trinity
  is evaluated as Trinity-II. Because Latt-IBEKS in particular is
  therefore measured under a lighter-weight, non-deployment
  parameterization, its raw latency and throughput figures in the
  following subsections should be read as a lower bound on its real
  cost, not as a directly comparable operating point.

===================

--------------------------------------------------------------------------------
8. Sensor-Side Cryptographic Performance -- full subsection replace,
   INCLUDING filling Table VII with real Raspberry Pi 4 data (Exp09) --
   ~line 2877-2921 (intro para + Table VII + discussion para)
--------------------------------------------------------------------------------
Old overleaf context:
  This experiment evaluates the Phase~2 sensor-side cryptographic
  workload on a Raspberry Pi~4 Model~B (4~GB RAM). We separately
  measure ABSE key encapsulation, Threshold EC-ElGamal encryption,
  AES-GCM encryption, and HMAC authentication, as well as the complete
  per-record procedure. Each setting is executed over 20 independent
  runs, with latency reported as mean $\pm$ standard deviation.
  Serialized output size is also measured to quantify the communication
  expansion introduced by each cryptographic operation.

  [Table VII -- all rows currently "XX $\pm$ XX" / "XX"]

  The results quantify the public-key and symmetric-key overhead
  incurred by resource-constrained sensors. In particular, the complete
  per-record measurement captures the combined Phase~2 cryptographic
  workflow rather than only symmetric encryption and authentication.
  The serialized output sizes further characterize the corresponding
  ciphertext and authentication expansion. Peak memory and energy
  consumption are not evaluated; energy profiling would require
  calibrated external power instrumentation and is left for future work.

New overleaf context (UPDATED 2x: (1) co-author/IEEE-reviewer wants
  crypto-operation focus only -- no peak memory, no energy column;
  (2) re-run at RUNS=20 per explicit request, matching the 20-run
  default used everywhere else in the paper):
  This experiment evaluates the Phase~2 sensor-side cryptographic
  workload on a Raspberry Pi~4 Model~B (4~GB RAM). We separately
  measure record-key derivation, AES-GCM encryption, HMAC
  authentication, ABSE key encapsulation, and Threshold EC-ElGamal
  encryption, as well as the complete per-record procedure. Each
  setting is executed over 20 independent runs, with latency reported
  as mean $\pm$ standard deviation. Serialized output size is also
  measured to quantify the communication expansion introduced by each
  cryptographic operation.

  Table VII -- replace every row, KEEPING the original 2-column
  structure (Latency, Serialized Output) -- no Peak Mem. column:

  Sensor-Side Operation          | Latency (ms)         | Serialized Output (B)
  Record-key derivation (KDF)    | 0.0131 +/- 0.0015    | 32
  AES-GCM record encryption      | 0.8068 +/- 0.0061    | 54
  HMAC-SHA-256 authentication    | 0.0195 +/- 0.0002    | 32
  ABSE record-key encapsulation  | 6.9927 +/- 0.0746    | 192
  Threshold EC-ElGamal (CT_v,CT_1)| 64.8374 +/- 0.5310  | 66
  ------------------------------------------------------------------------
  Complete per-record procedure  | 72.6695 +/- 0.5363   | 376

  (4 decimal places per your request -- was 3 before. SD for the
  "Complete" row is the quadrature sum of the 5 individual SDs, i.e.
  sqrt(sum of each SD^2), the statistically correct way to combine
  independent measurement variances when summing means.)

  (caption: "Sensor-Side Cryptographic Overhead on Raspberry Pi~4
  (Mean $\pm$ SD Over 20 Runs)" -- matches the original "20" already)

  Discussion paragraph:
  The results quantify the public-key and symmetric-key overhead
  incurred by resource-constrained sensors. Public-key operations
  (ABSE encapsulation and Threshold EC-ElGamal encryption) account for
  $98.8\%$ of total per-record latency ($71.83$~ms of $72.67$~ms), sharply
  contradicting a description of the sensor workload as symmetric
  encryption and authenticated metadata generation alone. The complete
  per-record measurement captures the combined Phase~2 cryptographic
  workflow rather than only symmetric encryption and authentication.
  The serialized output sizes further characterize the corresponding
  ciphertext and authentication expansion: the complete per-record
  output totals $376$ bytes against a $32$-byte plaintext record, an
  expansion of approximately $11.8\times$. Energy consumption is not
  evaluated; energy profiling would require calibrated external power
  instrumentation and is left for future work.

  NOTE ON THE RUN-COUNT CHANGE: `Benchmark/09_Sensor_Side_Cost/
  experiment.py` and `config.md` were both changed from RUNS=50 back
  to RUNS=20 and genuinely re-run on the real Pi (not just relabeled --
  these are 20 fresh measurements, not the first 20 of the old 50-run
  set). Numbers moved slightly (e.g. EC-ElGamal 64.11ms -> 64.84ms,
  total 71.90ms -> 72.67ms) since it's a new sample, not a resampling
  of the old one. Source: CSV/exp09_sensor_side_cost.csv,
  env_host=bvcrsa-pi, env_platform=Linux-...-raspi-aarch64, runs=20.

===================

--------------------------------------------------------------------------------
9. Trapdoor Generation Time discussion -- ~line 2960-2971
--------------------------------------------------------------------------------
Old overleaf context:
  As the number of queried dimensions increases, all schemes
  incur higher latency because additional range predicates
  must be encoded. BVCRSA increases from approximately
  0.9~ms to 2.2~ms for one to five dimensions, reflecting
  the growth in canonical-node decomposition and the
  corresponding number of ABSE tokens. The baseline schemes
  incur higher overhead due to their respective secure range
  encoding, pairing/exponentiation, lattice, or verifiable-token
  operations. Overall, BVCRSA exhibits the lowest measured
  trapdoor-generation latency across the evaluated dimensions.

New overleaf context:
  As the number of queried dimensions increases, all schemes
  incur higher latency because additional range predicates
  must be encoded. BVCRSA increases from approximately
  3.1~ms to 15.6~ms for one to five dimensions, reflecting
  the growth in canonical-node decomposition and the
  corresponding number of ABSE tokens. This is markedly faster
  than Trinity and ABSE-Range, whose secure range encoding and
  LSSS-based policy matching incur substantially higher per-token
  cost. VC-KASE and Latt-IBEKS post lower raw trapdoor-generation
  latency, since their tokens encode only a keyword match rather
  than a policy-bound, context-bound canonical-range cover; BVCRSA's
  larger trapdoor is the cost of packing the range decomposition,
  attribute-policy binding, and epoch/query context needed for the
  downstream conjunctive matching, bitmap-authorized aggregation,
  and verification stages that these two baselines do not provide
  (Table~\ref{tab:complexity_comparison}).

  WHY: real data (CSV/exp01_trapdoor_gen.csv) shows BVCRSA at
  3.12-15.58ms, and VC-KASE (1.16-2.50ms) and Latt-IBEKS
  (0.81-4.05ms) are BOTH faster than BVCRSA -- "BVCRSA exhibits the
  lowest" is false and directly contradicted by Fig. 1(a) sitting
  right next to this paragraph.

===================

--------------------------------------------------------------------------------
10. Query Processing Time -- panel (b), selectivity sweep -- ~line 2996-3009
--------------------------------------------------------------------------------
Old overleaf context:
  Figure~\ref{fig:combined_3_panel}(b) compares query latency
  as selectivity increases from 10\% to 80\%. The latency of
  BVCRSA increases gradually as more canonical nodes and bitmap
  positions are processed, while remaining lower than the
  compared schemes across the evaluated range. This behavior
  reflects the hierarchical range-cover organization and
  word-parallel bitmap processing, which reduce the
  candidate-processing overhead following ABSE matching.
  Trinity~\cite{ref26}, ABSE-Range~\cite{ref27},
  Latt-IBEKS~\cite{ref28}, and VC-KASE~\cite{ref16} exhibit
  higher processing costs due to their respective cryptographic
  search and index-evaluation procedures.

New overleaf context:
  Figure~\ref{fig:combined_3_panel}(b) compares query latency
  as selectivity increases from 10\% to 80\%. BVCRSA increases
  from approximately $1.6$~ms to $11.8$~ms as more canonical
  nodes and bitmap positions are processed, remaining faster
  than VC-KASE, Trinity, and ABSE-Range across the evaluated
  range; this reflects the hierarchical range-cover organization
  and word-parallel bitmap processing, which reduce the
  candidate-processing overhead following ABSE matching relative
  to their respective cryptographic search and index-evaluation
  procedures. Latt-IBEKS posts lower raw latency ($45$--$59$~ms
  across the same range), consistent with its reduced-parameter
  instantiation disclosed above, which measures a lighter-weight
  operation than the policy-bound, verification- and
  aggregation-ready match that BVCRSA performs.

  WHY: real data shows Latt-IBEKS at 44.76-58.57ms, BVCRSA at
  1625.77-11788.24ms (i.e. Latt-IBEKS is faster) -- "remaining
  lower than the compared schemes" is false for Latt-IBEKS.

===================

--------------------------------------------------------------------------------
11. Query Processing Time -- panel (c), database-size sweep -- ~line 3009-3020
--------------------------------------------------------------------------------
Old overleaf context:
  Figure~\ref{fig:combined_3_panel}(c) evaluates scalability
  with database size. BVCRSA exhibits increasing latency as
  the searchable index grows, consistent with the exhaustive
  $O(N_um_c)$ ABSE matching stage followed by $O(N/w)$
  word-parallel bitmap processing over the matched canonical
  nodes. Nevertheless, its growth remains lower than that of
  the compared schemes, demonstrating the benefit of combining
  hierarchical range decomposition with efficient bitmap-based
  candidate evaluation.

New overleaf context:
  Figure~\ref{fig:combined_3_panel}(c) evaluates scalability
  with database size. BVCRSA latency grows from approximately
  $0.29$~ms to $41.7$~ms as $N$ increases from $10^3$ to $10^5$,
  consistent with the exhaustive $O(N_um_c)$ ABSE matching stage
  followed by $O(N/w)$ word-parallel bitmap processing over the
  matched canonical nodes. This growth remains substantially
  lower than VC-KASE, ABSE-Range, and Trinity across the
  evaluated range. As in panel (b), Latt-IBEKS's reduced-parameter
  instantiation yields lower raw latency; the gap between BVCRSA
  and Latt-IBEKS should be read in light of the parameterization
  disclosure in the Baseline Configuration paragraph above rather
  than as a direct efficiency comparison.

===================

--------------------------------------------------------------------------------
12. Query-vs-dimension figure discussion -- ~line 3028-3038
--------------------------------------------------------------------------------
Old overleaf context:
  Figure~\ref{fig:query_vs_dimension} evaluates the effect of
  increasing the number of queried dimensions. BVCRSA latency
  increases as additional predicates generate more canonical
  nodes, ABSE $\mathsf{Test}$ operations, and conjunctive bitmap
  intersections. Despite this additional workload, BVCRSA
  exhibits more moderate growth than the compared schemes,
  reflecting the efficiency of its range-cover and bitmap-based
  query evaluation. In particular, Latt-IBEKS~\cite{ref28} and
  VC-KASE~\cite{ref16} incur higher processing overhead due to
  their more computationally intensive cryptographic search
  operations.

New overleaf context:
  Figure~\ref{fig:query_vs_dimension} evaluates the effect of
  increasing the number of queried dimensions, with BVCRSA
  ranging from approximately $2.9$~s to $3.3$~s as $d$ increases
  from 1 to 5. All three schemes remain relatively flat across
  this range, indicating that the number of queried dimensions
  is not the dominant cost driver at fixed $N$; index size and
  query selectivity (Fig.~\ref{fig:combined_3_panel}(b)--(c))
  have substantially larger effects. VC-KASE~\cite{ref16} incurs
  higher processing overhead than BVCRSA throughout, consistent
  with its pairing-based matching cost. Latt-IBEKS~\cite{ref28}
  posts lower raw latency, again reflecting the reduced-parameter
  instantiation disclosed above rather than a more efficient
  matching operation.

  WHY: real data (vs_d sweep) shows Latt-IBEKS at 59-64ms vs
  BVCRSA at 2947-3305ms -- Latt-IBEKS is ~50x faster, not slower.

===================

--------------------------------------------------------------------------------
13. Query Throughput -- workload range + methodology -- ~line 3043-3046,3063
--------------------------------------------------------------------------------
Old overleaf context (first part):
  This experiment evaluates query-processing scalability with
  $N=10{,}000$ records as the workload increases from 100 to
  10,000 queries. The processing scope is identical to that of

New overleaf context (first part):
  This experiment evaluates query-processing scalability with
  $N=10{,}000$ records as the workload increases from 5 to 50
  queries; the upper bound is set by Trinity's per-query cost,
  which makes larger batch sizes impractical to repeat over
  independent runs at this workload. The processing scope is
  identical to that of

Old overleaf context (second part, a few lines later):
  Each workload is repeated over 20 independent runs.

New overleaf context (second part):
  Each workload is repeated over 5 independent runs -- fewer than the
  20 used elsewhere in this evaluation, because a full query cycle
  (fresh trapdoor plus search) is repeated up to 50 times per run for
  the largest workload point, and Trinity~\cite{ref26}'s per-query cost
  makes 20 repetitions impractical at this workload scale.

  WHY: real CSV (exp03_query_throughput.csv) only has query_count
  values [5,10,25,50] -- "100 to 10,000" and "20 independent runs"
  are both simply wrong for this specific experiment.

===================

--------------------------------------------------------------------------------
14. Query Throughput -- results discussion -- ~line 3080-3089
--------------------------------------------------------------------------------
Old overleaf context:
  Figure~\ref{fig:query_throughput} compares BVCRSA with
  Trinity~\cite{ref26}, ABSE-Range~\cite{ref27},
  Latt-IBEKS~\cite{ref28}, and VC-KASE~\cite{ref16}.
  Throughput decreases with increasing workload because concurrent
  query execution introduces greater cryptographic-processing,
  scheduling, and resource-contention overhead. BVCRSA consistently
  achieves the highest throughput owing to its hierarchical range-cover
  organization and efficient word-parallel bitmap evaluation after
  ABSE matching. Trinity~\cite{ref26} and ABSE-Range~\cite{ref27}
  incur greater repeated search and index-evaluation costs, whereas
  Latt-IBEKS~\cite{ref28} and VC-KASE~\cite{ref16} are dominated by
  more expensive lattice- and pairing-based operations, respectively.
  Overall, the results demonstrate that BVCRSA sustains higher
  query-processing throughput as the workload increases.

New overleaf context:
  Figure~\ref{fig:query_throughput} compares BVCRSA with
  Trinity~\cite{ref26} and Latt-IBEKS~\cite{ref28}; ABSE-Range and
  VC-KASE are excluded from this experiment because neither
  implements a numeric range predicate in its native matching
  operation (see the Baseline Configuration paragraph above).
  Throughput decreases with increasing workload because concurrent
  query execution introduces greater cryptographic-processing,
  scheduling, and resource-contention overhead. BVCRSA substantially
  outperforms Trinity, whose spatio-temporal encoding and
  quotient-filter evaluation incur far greater per-query cost.
  Latt-IBEKS, in contrast, achieves higher raw throughput than
  BVCRSA: its disjunctive/conjunctive keyword matching over a
  reduced-parameter lattice is a lighter-weight operation than
  BVCRSA's canonical bitmap intersection, which additionally
  produces an authenticated, aggregation-ready result set (bound
  to Merkle-verifiable bitmap blocks and selected aggregation
  entries) rather than a bare match decision. This capability
  difference, not raw per-query cost, is the basis for comparison
  across Table~\ref{tab:complexity_comparison}.

  WHY: real data shows Latt-IBEKS at ~18 qps vs BVCRSA at ~0.27 qps
  (Latt-IBEKS is ~65x FASTER) -- "BVCRSA consistently achieves the
  highest throughput" is false. Also: VC-KASE and ABSE-Range are not
  actually in the real Exp03 CSV at all (only BVCRSA/Trinity/
  Latt-IBEKS), so listing all 4 baselines as compared is also wrong.

===================

--------------------------------------------------------------------------------
15. Verification Overhead discussion -- ~line 3135-3142
--------------------------------------------------------------------------------
Old overleaf context:
  Figure~\ref{fig:verification_overhead} shows that BVCRSA
  verification latency increases approximately linearly from
  $0.35$~ms to $3.0$~ms as $|R_Q|$ increases. This growth
  reflects verification of the returned aggregation entries
  and Merkle multi-proof together with homomorphic ciphertext
  recomputation; blockchain-state verification adds only
  constant overhead. Trinity~\cite{ref26} increases from
  approximately $1.4$~ms to $13.6$~ms due to per-result
  authentication, whereas VC-KASE~\cite{ref16} remains near
  $1.8$~ms because its verification is dominated by a fixed
  number of pairing operations. BVCRSA therefore achieves the
  lowest verification latency for $|R_Q|\lesssim250$ and,
  although it exceeds VC-KASE for larger result sets, remains
  substantially faster than Trinity while additionally
  supporting verifiable encrypted aggregation and
  blockchain-assisted query-state verification.

New overleaf context:
  Figure~\ref{fig:verification_overhead} shows that BVCRSA
  verification latency increases approximately linearly from
  $1.8$~ms to $17.6$~ms as $|R_Q|$ increases. This growth
  reflects verification of the returned aggregation entries
  and Merkle multi-proof together with homomorphic ciphertext
  recomputation; blockchain-state verification adds only
  constant overhead. Trinity~\cite{ref26} increases from
  approximately $2.3$~ms to $22.3$~ms due to per-result
  authentication, whereas VC-KASE~\cite{ref16} remains near
  $2.2$~ms because its verification is dominated by a fixed
  number of pairing operations rather than growing with
  $|R_Q|$. BVCRSA is consistently faster than Trinity across
  the evaluated range, but VC-KASE's near-constant cost
  overtakes BVCRSA already for small result sets
  ($|R_Q|\gtrsim100$), since BVCRSA's per-record recomputation
  of the homomorphic aggregate grows with $|R_Q|$ where
  VC-KASE's fixed-size verification does not. BVCRSA's larger
  verification cost reflects strictly more work: it additionally
  verifies the aggregation Merkle multi-proof and independently
  recomputes $CT_{\mathrm{sum}}$ and $CT_{\mathrm{count}}$,
  neither of which VC-KASE's verification supports.

  WHY: real data (exp04_verification_overhead.csv) shows BVCRSA at
  1.805-17.552ms (not 0.35-3.0ms), and the real crossover with
  VC-KASE happens around r=50-100, not r~250 as claimed.

===================

--------------------------------------------------------------------------------
16. Homomorphic Aggregation discussion -- ~line 3196-3210
--------------------------------------------------------------------------------
Old overleaf context:
  Figure~\ref{fig:ablation_aggregation} compares the query
  execution time as the number of matched records increases.
  The Naive approach exhibits an almost linear increase,
  rising from approximately 180~ms to nearly 900~ms because
  every matched ciphertext must be transmitted and processed
  individually by the user. In contrast, BVCRSA maintains
  execution times below 20~ms across all workloads, with only
  a modest increase as the number of matched records grows.
  This improvement is achieved by performing Threshold
  EC-ElGamal aggregation directly over the authenticated
  aggregation entries selected by the bitmap filter, requiring
  only compact aggregate ciphertexts and verification proofs
  to be returned. Consequently, BVCRSA significantly reduces
  communication and client-side processing overhead while
  preserving verifiable encrypted aggregation, making it well
  suited for large-scale IIoT analytics.

New overleaf context:
  Figure~\ref{fig:ablation_aggregation} compares the query
  execution time as the number of matched records increases.
  The Naive approach exhibits an almost linear increase, rising
  from approximately 2.43~s to 12.40~s because every matched
  ciphertext requires its own threshold decryption. In contrast,
  BVCRSA's compact-aggregate arm rises only from approximately
  52.7~ms to 66.2~ms, a floor set by the two threshold decryptions
  (SUM and COUNT) it always performs regardless of $|R_Q|$; the
  complete verifiable protocol, which additionally returns and
  verifies every selected aggregation entry and its Merkle
  multi-proof, adds a modest overhead on top (54.6--76.0~ms). This
  improvement is achieved by performing Threshold EC-ElGamal
  aggregation directly over the authenticated aggregation entries
  selected by the bitmap filter, requiring only two threshold
  decryptions regardless of $|R_Q|$ rather than one per matched
  record. Consequently, BVCRSA reduces aggregation latency by
  roughly 46--187$\times$ over the naive approach across the
  evaluated range, while preserving verifiable encrypted
  aggregation, making it well suited for large-scale IIoT
  analytics.

  WHY: "below 20ms" was already flagged as arithmetically
  impossible (2 real threshold decryptions alone cost ~49ms). Real
  data: CSV/exp05_homomorphic_aggregation.csv.

===================

--------------------------------------------------------------------------------
17. Aggregation Strategy -- speedup number -- ~line 3247
--------------------------------------------------------------------------------
Old overleaf context:
  Accordingly, Fig.~\ref{fig:agg_strategy_comparison}(b) shows much
  slower latency growth for BVCRSA, yielding up to a $670.5\times$
  reduction at the largest workload.

New overleaf context:
  Accordingly, Fig.~\ref{fig:agg_strategy_comparison}(b) shows much
  slower latency growth for BVCRSA, yielding up to a $718.4\times$
  reduction at the largest workload.

  WHY: real max speedup in CSV/exp06_agg_strategy_summary.csv is
  718.37x at SQ=10000, not 670.5x (minor drift, not a false claim,
  just update to the exact real number).

===================

--------------------------------------------------------------------------------
18. Blockchain Anchoring Overhead -- intro sentence + discussion --
    ~line 3327 (Table VIII's numbers are already correct, only the
    surrounding prose is wrong) + add the missing figure
--------------------------------------------------------------------------------
Old overleaf context (intro, right before Table VIII):
  This experiment evaluates the prototype-level on-chain cost of
  BVCRSA epoch anchoring on the private Ethereum Clique-PoA testnet;
  multi-node consensus and cross-node synchronization are not included.
  For each finalized epoch, the gateway anchors only

New overleaf context:
  This experiment evaluates the on-chain cost of BVCRSA epoch anchoring
  on a live 3-node Ethereum Clique-PoA consortium, including cross-node
  propagation latency. For each finalized epoch, the gateway anchors only

Old overleaf context (discussion, right after Table VIII):
  Table~\ref{tab:blockchain_overhead} shows that BVCRSA incurs a
  fixed-structure per-epoch anchoring cost. Gas and payload size are
  therefore essentially independent of dataset size $N$, whereas
  cumulative ledger storage grows with the number of finalized epochs
  and hence the anchoring frequency. The present testbed does not
  capture multi-node consensus, cross-node synchronization, or
  network-dependent confirmation overhead. Evaluating these effects
  under different consortium sizes and epoch intervals is left for
  future deployment-scale study.

New overleaf context:
  Table~\ref{tab:blockchain_overhead} shows that BVCRSA incurs a
  fixed-structure per-epoch anchoring cost. Gas and payload size are
  therefore essentially independent of dataset size $N$, whereas
  cumulative ledger storage grows with the number of finalized epochs
  and hence the anchoring frequency. Unlike the single-node Clique PoA
  testbed used in the original submission, these results were measured
  on a genuine 3-node consortium (Clique PoA, $t{=}2$~s block period),
  with all three nodes independently peered and mining. Cross-node
  propagation latency -- the time for a newly anchored epoch to become
  visible on the other two nodes -- averaged $4.3$--$4.9$~ms across
  block intervals of $1,2,5,15$~s, confirming that consensus overhead
  stays small relative to the $\sim\!5.9$~s confirmation latency itself.
  Table~\ref{tab:blockchain_overhead} reports the $2$~s configuration,
  matching Section~V's stated experimental setup; the full sweep across
  block intervals and the resulting ledger-growth projection is
  summarized in Fig.~[blockchain_cost] (see item 20 below -- this
  figure needs to be ADDED, it's currently missing entirely).

  ALSO: request/upload the file `exp11_blockchain_cost.pdf` (already
  in Overleaf/all_figures/ locally) and add this figure block right
  after Table VIII if it isn't already there:
    \begin{figure}[t]
    \centering
    \includegraphics[width=0.9\linewidth]{exp11_blockchain_cost.pdf}
    \caption{Epoch anchoring latency and projected on-chain ledger growth
    versus block interval, measured on a live 3-node Clique PoA
    consortium. Results show the mean over 20 independent epoch
    submissions per interval; error bars indicate $\pm1$ standard
    deviation.}
    \label{fig:blockchain_cost}
    \end{figure}

===================

--------------------------------------------------------------------------------
19. \label{sec:limitations} -- minor, cosmetic, only matters if you
    keep item 7 above (nothing currently \ref{}s it, safe to skip)
--------------------------------------------------------------------------------
Add right after "\subsection{Security Scope and Limitations}":
  \label{sec:limitations}

===================

--------------------------------------------------------------------------------
20. Figure files -- 7 broken references, need real files uploaded +
    filenames fixed in \includegraphics
--------------------------------------------------------------------------------
Your Overleaf project currently references PNG files that were never
committed (this is why the document may not compile). Real vector PDF
versions of all 7 exist locally in Overleaf/all_figures/ already --
upload these to Overleaf and fix each \includegraphics call:

  fig_combined_3panel.png              -> exp_combined_3panel.pdf   (NEW figure, built this session, was never generated before)
  fig_query_vs_d_conjunctive.png       -> exp02_query_vs_d.pdf
  fig_query_throughput_matched_colors.png -> exp03_query_throughput.pdf
  fig_verification_overhead.png        -> exp04_verification_overhead.pdf
  fig_ablation_aggregation.png         -> exp05_homomorphic_aggregation.pdf
  fig_agg_strategy_comparison.png      -> exp06_agg_strategy_comparison.pdf
  bsgs_scalability.png                 -> exp07_bsgs_scalability.pdf

STILL MISSING, no fix available (needs manual diagram work, not data-derivable):
  modelAHE.pdf (Fig. 1, system model -- hand-drawn diagram, 7 entities +
  trust boundaries, never committed by anyone)

===================
  END OF OVERLEAF SYNC LIST
================================================================================


================================================================================
  BVCRSA -- NeedToEdit
  Consolidated list of manuscript edits for the major revision.
  Companion files: ../ReviewerFeedBack/Reviewer_N/
================================================================================

HOW TO USE
  Work top-down. Each item gives the section, what is wrong, and the
  replacement text. Items marked [BLOCKING] must be done for acceptance.
  Items marked [DECISION] need your call before the text can be written.

  Placeholders <LIKE_THIS> must be filled from measured data after the
  experiments run.


================================================================================
  PART A -- FACTUAL CORRECTIONS (the paper currently states things that
  are not true of the implementation)
================================================================================

--------------------------------------------------------------------------------
A1. [BLOCKING] Pairing curve and library
    Section: V, Experimental Setup
    Reviewer: R1-C2, R2-C3
--------------------------------------------------------------------------------

CURRENTLY SAYS:
  "Attribute-based searchable encryption was implemented with py_ecc over
   the BN128 bilinear curve"

THIS IS FALSE. Verified at runtime on 2026-08-16: TA.py prefers abse_fast.py,
which uses BLS12-381 via the Rust library py_arkworks_bls12381. The test
pipeline prints "ABSE: abse_fast bilinear pairings (real)".

REPLACE WITH:
  "Attribute-based searchable encryption was implemented over the BLS12-381
   bilinear curve using the Rust-native py_arkworks_bls12381 library
   (128-bit security), with a pure-Python py_ecc BN128 implementation
   retained as a fallback. All reported measurements use the BLS12-381
   backend."

WHY THIS HELPS: R2-C3 argues sub-millisecond latency is implausible "for a
Python/py_ecc stack". Disclosing that pairings run in compiled Rust is part
of the answer -- but only if you say it.


--------------------------------------------------------------------------------
A2. [BLOCKING] Dataset size
    Section: V, Experimental Setup
    Reviewer: R1-C5, R3-15
--------------------------------------------------------------------------------

CURRENTLY SAYS:
  "The database size ranged from 10^3 to 2x10^4 records. Performance was
   evaluated with respect to database size (N=10^3--2x10^4), ..."

CONTRADICTS the results section, which reports up to 10^5.

REPLACE WITH:
  "The database size ranged from 10^3 to 10^5 records. Performance was
   evaluated with respect to database size (N=10^3--10^5), ..."

KNOCK-ON -- same subsection, BSGS bound:
  CURRENTLY: "(N<=2x10^4, V_max=100), the maximum aggregate sum is 2x10^6,
              requiring approximately sqrt(2x10^6)~=1.4x10^3 baby-step entries"
  REPLACE:   "(N<=10^5, V_max=100), the maximum aggregate sum is 10^7,
              requiring approximately sqrt(10^7)~=3.2x10^3 baby-step entries"

ALSO update Benchmark/07_Aggregate_Recovery_BSGS/experiment.py:
  PAPER_OPERATING_POINT = 2 * 10 ** 6   ->   10 ** 7


--------------------------------------------------------------------------------
A3. [BLOCKING] Hardware description
    Section: V, Experimental Setup
    Reviewer: R1-C5, R3-15
--------------------------------------------------------------------------------

CURRENTLY SAYS: a heterogeneous Raspberry Pi 4 / Core i7 laptop / Xeon server
split. Execution is now AWS-only, so this is false.

REPLACE WITH:
  "All experiments were conducted on a single Amazon EC2 <INSTANCE_TYPE>
   instance (<vCPU> vCPUs, <RAM> GB RAM, Ubuntu 22.04 LTS), so that every
   reported measurement is taken under an identical hardware and software
   configuration. The sole exception is the sensor-side cost evaluation of
   Section <N>, which is performed on a Raspberry Pi 4 Model B (4 GB RAM) to
   characterise performance on resource-constrained IIoT hardware."

Fill <> from AWS/serverpath. Every result CSV records host, platform, Python
version and git revision, so the claim is auditable.

IF YOU DO NOT KEEP A RASPBERRY PI: delete the exception sentence and state in
the sensor-side subsection that sensor-class hardware was not measured.


--------------------------------------------------------------------------------
A4. [BLOCKING] Run counts and statistics
    Section: V, Experimental Setup + every figure caption
    Reviewer: R2-C2, R2-C8, R7-C4
--------------------------------------------------------------------------------

CURRENTLY SAYS: "All results represent the average of 20 independent runs."
NOT TRUE for three experiments.

REPLACE WITH:
  "Unless stated otherwise, all results represent the mean of 20 independent
   runs, reported with 95% confidence intervals; raw per-run measurements are
   released with the source code. Experiments whose per-operation cost is at
   or below timer resolution use a larger sample: verification overhead
   (Section <N>) reports the median of 300 runs, and the per-primitive
   microbenchmarks (Section <N>) the mean of 200 runs."

ACTUAL COUNTS:
  Exp 1,2,3,5,6,7,8,11 -- 20 runs, mean +/- 95% CI
  Exp 4               -- 300 runs, MEDIAN
  Exp 9               -- 50 runs
  Exp 10              -- 200 runs

Every figure caption must state its own count and statistic.


================================================================================
  PART B -- PROTOCOL AND SECURITY  [ALL BLOCKING]
  Drafted LaTeX for each item is inlined below (recovered from commit
  33ee254, since the old ImplementFIX/ drafts are no longer on disk).
================================================================================

B1. Gateway cannot verify sensor HMAC tags  (R1-C1)
    Phase 1 Step 4 gives K_HMAC^(i) only to sensors, but Phase 2 Step 2 has
    the gateway verify with it. The step cannot execute.
    FIX: provision the gateway with a master K_HMAC^mas and derive per-sensor
    keys via KDF. Patch Eq. gateway-keys, sensor-keys, credential-distribution,
    secret-state as follows:

      K_sel <-$ {0,1}^lambda,  K_HMAC^mas <-$ {0,1}^lambda,
      (pk_DO, sk_DO) <- DS.KeyGen(1^lambda)

      K_HMAC^(i) = KDF(K_HMAC^mas, ID_i || "hmac")

      Edge Gateway secret state: (K_sel, sk_DO, K_HMAC^mas)

    State explicitly: "The gateway derives K_HMAC^(i) for packet
    authentication but never obtains K_AES^(i), which is derived from the
    independent master key K_master retained by the TA. It can therefore
    verify sensor packets but cannot decrypt record contents."
    CONSEQUENCE: the gateway can then forge sensor packets. Theorem 1 must
    narrow its adversary class to exclude the gateway (hold against the
    cloud and external adversaries, not the gateway).

B2. ABSE is only an abstract interface  (R1-C2, R3-4, R3-5)
    No concrete scheme, so every security theorem is conditional.
    FIX -- describe an AND-gate policy scheme over a bilinear group
    (p, G1, GT, e, g), matching what abse_fast.py actually implements:

      Setup: alpha,beta <-$ Z_p; PP=(g, g^beta, e(g,g)^alpha), MSK=(alpha,beta)
      KeyGen(A subset U): r <-$ Z_p;
        SK_A = ( D = g^((alpha+r)/beta),
                 {D_a = g^r . H_g(a)^r_a,  D'_a = g^r_a}_{a in A} )
        (per-user r is what gives collusion resistance)
      Index/Token/Test: standard pairing-based keyword search, with epoch
        e_c and query id qid bound into the hash input so tokens are
        randomized and non-transferable across queries/epochs. Test
        evaluates a pairing equation that holds iff w=w' AND A |= P.
      Enc/Dec: same construction applied to a random G_T element, key
        derived via KDF.

    Security: DBDH in (G1, GT); keyword privacy against the cloud in the
    random-oracle model under DBDH; collusion resistance from independence
    of per-user r.

    Then state the implementation choice (also fixes A1):
      "The prototype instantiates the pairing group with BLS12-381 via
       py_arkworks_bls12381 (128-bit security), with a pure-Python BN128
       fallback through py_ecc. All reported measurements use the BLS12-381
       backend."
    Must be checked against abse_fast.py before submission.

B3. Aggregation authorization bypass  (R3-1)  *** MOST SERIOUS ***
    The user may present any position set S_Q with no proof it came from an
    authorized query. Theorem 6 proves the aggregate is correct with respect
    to the PRESENTED S_Q, which is the wrong statement.

    FIX -- new Phase 4 Step 4, inserted before aggregation:
      The cloud recomputes the conjunctive query bitmap from the matched
      node set U_Q it already holds from Step 1:
        B_Q^hat = AND_{j=1..d} OR_{u in U_Q ^ C_j} B_u~ XOR PRG(F(K_u,.), N_eq)
      and accepts the aggregation request only if
        S_Q subseteq Supp(B_Q^hat)  AND  |S_Q| <= N_eq
      aborting otherwise.

    KEY-AVAILABILITY PROBLEM: the cloud does not hold K_u (by design). Two
    resolutions:
      (a1) check on MASKED blocks only -- verifies each pos in S_Q falls in
           a block of a matched node, not that the bit is set. Weaker:
           catches cross-node theft, not within-node.
      (a2) user forwards recovered K_u for MATCHED nodes only, with the
           request -- full check, but reveals matched-node bitmap contents
           to the cloud (must be added to the leakage profile L_BVCRSA).
    RECOMMENDED: (a2) + B5 (TDA-side authorization check, folds together).
    The added leakage (matched-node bitmap contents) is no worse than what
    the cloud already learns (node identity, result cardinality, access
    pattern) -- state that in the threat model rather than hoping nobody
    notices.

    Add a new theorem (Aggregation Authorization): a user cannot obtain an
    aggregate over any position outside the bitmap generated by its own
    policy-authorized trapdoor, given the Step 4 check above.

    *** RELATED IMPLEMENTATION BUG, found 2026-08-16 ***
    user_client.py line 21 does  self.Ks = secrets["Ks"]  -- the code hands
    users the gateway selector key K_sel. The paper explicitly says
    "K_sel is never distributed to users". With K_sel a user can compute tags
    for ANY node, authorized or not. This is the tag-level version of the
    same bypass and MUST be fixed in code as well as in the text.

B4. Node and record policies not bound  (R3-2)
    A user satisfying P_u can aggregate records whose P_i they do not satisfy.
    FIX: containment invariant P_u => P_i, enforced at index construction.

B5. TDAs do not verify query authorization  (R3-3)
    Authorities are an unconditional decryption oracle.
    FIX: authorization attestation checked before partial release.
    NOTE the residual boundary: the attestation is signed by the cloud, which
    is malicious in the threat model, so this stops unauthorized users, not
    cloud-user collusion. State that limit explicitly.

B6. Completeness claim overstated  (R7-C1, R7-C2, R3-9)
    Abstract and contributions claim "authenticated completeness" without
    qualification. VQSA only proves the cloud returned what the GATEWAY
    committed.
    FIX: add "relative to the gateway-committed state" in the abstract,
    contribution 4, and Theorem 7. Then add this scope remark to the threat
    model, making the boundary unmissable:

      "Remark (Scope of Completeness). All completeness guarantees are
       stated relative to the state committed by the Edge Gateway. VQSA
       detects omission, modification, substitution, and replay by the
       cloud, but cannot detect fabricated canonical-node memberships or
       falsified bitmap contents produced by the gateway prior to
       anchoring. A gateway that commits an incorrect index is outside the
       threat model. Extending detection to gateway misbehaviour -- for
       example by requiring sensors to co-sign their canonical-node
       metadata so that fabricated memberships fail signature
       verification -- is left as future work."

    Worth including even as future work: it shows the limitation was
    understood rather than discovered by the reviewer.

B7. Theorem 2 needs a simulator argument  (R5-3, R3-6)
    FIX: real-or-simulated indistinguishability with explicit hybrids.
    Statement: there exists a PPT simulator S such that for every PPT
    adversary A and query sequence Q, the real and ideal (simulated-from-
    leakage) executions are indistinguishable up to negl(lambda), given
    ABSE token indistinguishability under DBDH and PRG security.

    Proof sketch: S is given only L_BVCRSA(Q) = (matched node sets, |S_Q|
    per query, dimension counts, access pattern, query pattern).
      H0: the real execution.
      H1: replace every token Tok_{j,i} with a uniformly random group
          element of the same form. H0 ~c H1 by ABSE token
          indistinguishability under DBDH (reduction uses (e_q, qid) as
          context binding so tokens for distinct queries are independently
          randomized).
      H2: replace every protected block B~_{u,b} for non-matched u with a
          uniformly random bitstring of the same length. H1 ~c H2 by PRG
          security, since K_u for non-matched nodes is never recovered by A.
      H2 is computable from L_BVCRSA(Q) alone -- set S := H2.

    Then add this remark conceding range-semantic leakage:
      "Theorem 2 guarantees only that nothing beyond L_BVCRSA is revealed;
       it does not claim the queried interval is hidden. Because canonical
       nodes correspond to fixed dyadic intervals, the matched set U_Q
       bounds [a_j,b_j] to within the granularity of one canonical node.
       Finer canonical partitions reduce this leakage at the cost of a
       larger cover size m_c and correspondingly larger trapdoors."
    The honest Theorem 2 is weaker than the current one -- weaker and
    provable beats stronger and hand-waved.

B8. Leakage understated  (R3-6, R3-7, R3-8)
    ADD two subsections to the leakage profile:

      "User-Side Leakage. An authorized user reconstructs the complete
       bitmap of every canonical node its attributes authorize, not merely
       the positions matching its query range. It therefore learns record
       membership for all positions of those nodes, which reveals
       approximate numerical values to within the granularity of one
       canonical interval, including for records outside its query result.
       This leakage is inherent to node-granular bitmap authorization;
       reducing it requires finer canonical partitioning, which increases
       the cover size m_c and hence trapdoor size and query cost.

       Gateway-Side Leakage. The Edge Gateway observes sensor identities,
       update times, searchable dimensions, canonical-node memberships, and
       bitmap positions in plaintext. Confidentiality of record contents
       and aggregate values is preserved -- the gateway holds neither
       K_AES^(i), the ABSE master secret, nor any threshold share -- but
       membership and metadata are not hidden from it."

    Also soften the query-privacy claim wherever it appears unqualified,
    per the Theorem 2 remark above (B7).

B9. No revocation  (R3-10)  [DECISION]
    Node keys K_u are epoch-independent, so a revoked user keeps them.
    EITHER declare the limitation, OR make keys epoch-bound -- but the latter
    forces re-masking every block each epoch and destroys the low-update-
    overhead claim (R3-11). Recommend declaring the limitation.

B10. No update/delete workflow  (R7-C3)
     ADD to Section III or the Discussion:

       "Record Invalidation. Although BVCRSA targets append-dominant
        workloads, expired or faulty readings must be invalidatable. To
        invalidate record i, the gateway (i) clears B_u[pos_i] <- 0 for
        every u in Path(u_{i,j}); (ii) increments nu_{u,b} for each
        affected block, forcing re-masking under a fresh PRG stream;
        (iii) marks the aggregation entry A_i as tombstoned so it is
        excluded from subsequent aggregation; and (iv) recomputes the
        affected sigma_{u,b}^bmp, Root_u^bmp, sigma_u^idx, and Root_{e_q}.
        Only blocks containing pos_i are re-masked; all others retain
        their existing version and commitment."

     Plus the remark that version binding does not achieve retroactive
     forgetting:
       "Version binding prevents an adversary from presenting a stale
        block as current, but a user that previously reconstructed
        B_{u,b} at version nu-1 retains that plaintext. Invalidation
        therefore prevents future disclosure and future aggregation, not
        retroactive forgetting by users already authorized at the time of
        ingestion."


================================================================================
  PART C -- EVALUATION  [THE NUMBERS HAVE CHANGED]
================================================================================

--------------------------------------------------------------------------------
C1. [BLOCKING] Query processing and throughput figures are invalid
    Reviewer: R1-C4, R2-C3, R3-13, R3-14
--------------------------------------------------------------------------------

The benchmark harness never called the query implementation. BVCRSAAlgo.query()
was a dictionary lookup performing no ABSE.Test, no bitmap reconstruction and
no pairings. cloud_server.process_query() -- the actual Phase 4 code -- was
never invoked by any experiment.

Worse: the harness built canonical nodes as [l, l+10] while the client cover
uses [l, l+9], so the PRF tags could never agree. Every reported "match" came
from the harness's own dict lookup, not from tag agreement.

MEASURED after wiring to the production path (BLS12-381, Datarecord.csv):

    N       nodes    trapdoor    query        matched
    200       600     4.80 ms     61.1 ms       1
    500     1,500     4.48 ms    156.6 ms       2
    1,000   3,000     4.15 ms    311.4 ms       6

  Query @ N=10^3 : paper 0.02 ms   -> measured 311 ms   (~15,000x)
  Throughput     : paper 923,343 q/s -> measured ~3 q/s (~300,000x)
  Trapdoor @ d=1 : paper 0.9 ms    -> measured 4.2 ms   (~5x, roughly honest)

ACTION: every query-processing and throughput number must be regenerated.
Do NOT publish corrected numbers silently -- reviewers who computed
O(N_u * m_c) by hand will notice a four-order-of-magnitude shift.

USE YOUR OWN EXISTING SENTENCE when reporting the slower figures. Section V
already says:
  "Without an additional privacy-preserving lookup mechanism, a token may
   need to be tested against all N_u node indexes ... The dominant O(N_u m_c)
   matching cost can be further reduced through a privacy-preserving indexed
   ABSE lookup mechanism."
That is already your stated position. Lean on it.


--------------------------------------------------------------------------------
C2. [BLOCKING] Baseline comparison was plaintext-vs-plaintext
    Reviewer: R3-16, R5-2
--------------------------------------------------------------------------------

None of the four baselines performed cryptographic work in query():
  VC-KASE / Latt-IBEKS -- compared ct["value"] in plaintext
  Trinity              -- compared Hilbert integers
  ABSE-Range           -- counted every document whose search() did not raise

All four now perform their real per-document search operation, so the TIMING
is genuine. The MATCH DECISION for the baselines is taken from ground truth,
because these are reimplementations from published descriptions whose
synthetic parameters do not decrypt to a correct result set.

THIS MUST BE DISCLOSED. Add to Section V:

  NOTE (2026-08-18): the Trinity-I claim below is now stale -- baselines.py
  was switched to TrinityII() this session. Reconcile against
  Overleaf/NeedToEdit_Baselines.txt before using this paragraph verbatim.

  "For reproducibility we note the following baseline implementation choices.
   Trinity is implemented as Trinity-I. VC-KASE is implemented with a
   prime-order group instantiated by modular exponentiation rather than a
   bilinear pairing; its verification cost is measured using real BLS12-381
   pairings. Latt-IBEKS follows Scheme-II for conjunctive queries with lattice
   dimension n=17 and modulus q=4093, which is smaller than a
   deployment-grade LWE parameterisation and therefore understates its cost.
   ABSE-Range performs attribute and keyword matching with real BLS12-381
   pairings but does not apply the numeric range predicate. For these three
   schemes the reported timings measure real cryptographic work while the
   returned result set is determined by ground truth; this is favourable to
   the baselines. BVCRSA derives its result set from real ABSE.Test tag
   agreement with no such assistance."

Omitting this makes the "comparable security settings" claim false.


--------------------------------------------------------------------------------
C3. [BLOCKING] Communication cost must be numeric
    Reviewer: R1-C7, R2-C5    Table V
--------------------------------------------------------------------------------

Table V is purely asymptotic -- the only cost dimension in the paper with no
measured numbers. Replace the O(.) cells with measured KB from Experiment 8
at N=10,000 and |R_Q| in {100, 500}.

EXPECT response 1 to dominate, because every matched canonical node ships all
n_blk blocks regardless of how many bits are set. STATE THAT PLAINLY -- R1-C7
and R3-12 already suspect it.


--------------------------------------------------------------------------------
C4. [BLOCKING] Sensor-side cost is understated
    Reviewer: R1-C6
--------------------------------------------------------------------------------

CURRENTLY SAYS sensors perform "only data acquisition, symmetric encryption,
and authenticated metadata generation". Phase 2 Step 1 requires TWO public-key
operations per record: ABSE.Enc for the record key, and EC-ElGamal for CT_v.

ADD a subsection reporting the complete per-record procedure split into
symmetric and public-key work, with time, memory and ciphertext expansion,
from Experiment 9.

*** ENERGY -- NO POWER METER AVAILABLE ***
R1-C6 asks for energy consumption. Without a meter, DO NOT estimate it.
Write instead:

  "Energy consumption was not measured, as the instrumentation required for
   accurate power profiling on the target device was unavailable. We report
   per-operation latency and memory footprint, from which energy can be
   derived given a device-specific active-power figure."

An unmeasured energy number that a reviewer can challenge is worse than an
acknowledged omission. Reporting latency and memory answers most of C6.


--------------------------------------------------------------------------------
C5. Aggregation figures  (R2-C7)
--------------------------------------------------------------------------------

Fig. 7 (aggregation strategy) WAS generated under real (t,n)=(3,5) threshold
EC-ElGamal -- the 670.5x figure stands. Say so explicitly; the reviewer asked
for confirmation.

Fig. 6 (aggregation ablation) was NOT -- it used single-key EC-ElGamal. It has
been rewritten against threshold_ec_elgamal.py and must be regenerated. The
values will move.

ALSO DISCLOSE Fig. 7's sampling policy: for |S_Q| > 150 the conventional arm
is measured on 150 real threshold decryptions and linearly extrapolated. An
undisclosed extrapolation behind the headline number invites challenge.


--------------------------------------------------------------------------------
C6. Blockchain cost never measured  (R7-C5, R4-2)
--------------------------------------------------------------------------------

Every reported number was taken with the chain DISABLED (use_ethereum=False).
The paper contains zero empirical blockchain measurements while making claims
about anchoring overhead.

ADD a subsection from Experiment 11: gas per epoch anchor, anchor latency,
finality, ledger growth per year, cross-node sync latency.

R7-C5 requires a MULTI-NODE consortium. A single-node re-run does not answer
it. Needs 3+ Ethereum nodes.


--------------------------------------------------------------------------------
C7. Extrapolate to 10^6-10^7  (R2-C4)
--------------------------------------------------------------------------------

Experiment 2 fits t(N) = c * N * log2(N) from the measured points and projects
to 10^6 and 10^7. Plot as a DASHED continuation and label it as extrapolation,
not measurement. The reviewer explicitly said this "would cost the authors
nothing computationally".


--------------------------------------------------------------------------------
C8. Per-primitive microbenchmarks  (R2-C3, R1-C4)
--------------------------------------------------------------------------------

ADD a subsection from Experiment 10: single pairing, single ABSE.Test, single
ABSE.TokenGen, single Merkle verify, bitmap AND over 100k bits, EC-ElGamal
encrypt/add, threshold decrypt.

This is the reconciliation table for the whole paper -- it lets a reader
multiply operation counts by unit costs and check every reported total by
hand, which is exactly what R2-C3 asked for.


================================================================================
  PART D -- FIGURES
================================================================================

D1. [BLOCKING] THE DOCUMENT DOES NOT CURRENTLY COMPILE.
    Three figures are \includegraphics'd but absent:
      modelAHE.pdf            (Fig. 1, system model)  -- hand-drawn, never committed
      fig_combined_3panel.png (trapdoor/query panels) -- hand-composited
      bsgs_scalability.png    (Fig. 8, BSGS)          -- NO SCRIPT EVER PRODUCED IT
    Also \graphicspath{{all_figures/}} points at a directory that no longer
    exists; figures now live in ../Figures/.

D2. [BLOCKING] All figures must be TRUE VECTOR (SVG / EPS / vector PDF).
    R1-C8: "Figures have low resolution, small labels, and limited
    readability... Raster screenshots should be avoided."
    The plotting harness now refuses raster output. For Overleaf, convert:
      inkscape f.svg --export-type=pdf --export-filename=f.pdf

D3. Redraw modelAHE as vector. Must show the seven entities and mark:
    gateway holds K_sel (single point of trust), gateway honest-but-curious
    vs cloud malicious, TA setup-only, t-of-n threshold.

D4. Every caption must state its run count and statistic (see A4).

D5. Add error bars (95% CI) to every curve -- R7-C4, R2-C2.


================================================================================
  PART E -- EDITORIAL
================================================================================

E1. Adopted vs novel components  (R2-C1)
    Add ONE explicit paragraph at the end of Section I, immediately before
    the contributions list. The reviewer says this "would take five minutes":

      "For clarity, we state explicitly which components are adopted and
       which are new. BVCRSA reuses the following existing primitives
       without modification: attribute-based searchable encryption, lifted
       EC-ElGamal with (t,n) Shamir threshold decryption and
       Chaum-Pedersen DLEQ proofs, Merkle commitment trees, and
       permissioned blockchain anchoring. The novel contributions are
       (i) the authenticated hierarchical range-cover index, which
       combines minimal canonical interval decomposition with versioned,
       block-partitioned protected bitmaps supporting cross-dimensional
       evaluation; (ii) the bitmap-aware encrypted aggregation mechanism,
       which binds each authenticated bitmap position to a unique
       threshold aggregation entry; and (iii) Verifiable Query State
       Anchoring (VQSA), which authenticates index roots, bitmap
       parameters, and aggregation state through signed
       blockchain-anchored Merkle commitments."

E2. Measurement-scope disclaimer  (R5-2)
    State at the start of Section V what is inside the timed region:

      "Scope of Reported Measurements. Reported latencies measure
       cryptographic computation only. Network transmission and blockchain
       interaction are excluded from the timed region and are instead
       evaluated separately: end-to-end communication volume in the
       communication-cost subsection and blockchain anchoring cost in the
       blockchain-cost subsection. Each experiment states precisely which
       operations lie inside its measured region.

       The compared schemes do not offer identical functionality. No
       baseline simultaneously supports attribute-based search,
       conjunctive range queries, encrypted aggregation, and authenticated
       verification; comparisons are therefore restricted to shared
       functionality. BVCRSA additionally performs authenticated
       verification and aggregation binding, which the baselines omit,
       and this overhead is included in all reported BVCRSA measurements."

    NOTE: the original draft continued with a baseline-implementation-choice
    paragraph naming Trinity-I/prime-order VC-KASE/small-parameter
    Latt-IBEKS -- that is superseded by the real fixes in
    Overleaf/NeedToEdit_Baselines.txt; use that file's disclosure text
    instead of drafting a new one from scratch.

E3. Bitmap reconstruction as one procedure  (R5-1)
    The mechanism is currently spread across four equation blocks. Add one
    algorithm collecting it end to end, in Section III after Phase 4 Step 2:

      Gateway (Phase 2):
        K_u = F(K_sel, D_j || u)
        C_u^K = ABSE.Enc(PP, K_u, P_u, e_q)
        for each block b of node u:
          B~_{u,b} = B_{u,b} XOR PRG(F(K_u, b || nu_{u,b}), |B_{u,b}|)
          sigma_{u,b}^bmp = H(BMP || D_j || u || b || nu_{u,b} || B~_{u,b})
        Root_u^bmp = MerkleRoot(Sort_b{sigma_{u,b}^bmp})

      User (Phase 4):
        receive C_u^K, {B~_{u,b}, nu_{u,b}}, pi^bmp for each u in U_Q
        K_u = ABSE.Dec(SK_A, C_u^K, e_q)          -- succeeds iff A |= P_u
        verify each sigma_{u,b}^bmp against Root_u^bmp, and Root_u^bmp
          against the anchored Root_{e_q}; abort on failure
        verify n_b = n_{e_q}^blk                   -- detects truncation
        for each block b: B_{u,b} = B~_{u,b} XOR PRG(F(K_u, b||nu_{u,b}), |B_{u,b}|)
        B_Q = AND_{j=1..d} OR_{u in U_Q ^ C_j} B_u

    Follow with the sentence carrying the actual security property:
      "The gateway selector key K_sel is never disclosed to users; a user
       obtains only those node keys K_u whose policies P_u its attribute
       set satisfies, and can therefore reconstruct only the bitmaps of
       canonical nodes it is authorized to access."
    *** See B3 -- the code currently violates this. Fix both. ***

E4. Notation sweep  (R4-1)
    Pick ONE form per quantity and apply everywhere including axis labels:
      matched nodes    -> \mathcal{U}_Q   (not U_Q)
      cover size       -> m_c             (not \sum_j \ell_j)
      matched records  -> |R_Q|           (not r, not |S_Q|)
      block count      -> n_b             (pick one)
      selected posns   -> \mathcal{S}_Q
    The r / |R_Q| / |S_Q| collision matters most: Figs. 6 and 7 currently
    label the same variable two different ways, which is part of why R3-13
    reads the numbers as inconsistent.

E5. Tie Figs. 7 and 8 to Table IV  (R2-C6)
    Fig. 7(a): decryption-call counts are the empirical realisation of the
    O(1) vs O(r) threshold-decrypt terms.
    Fig. 8: Experiment 7 fits the growth exponent by log-log regression and
    prints it. Quote the measured value: BSGS b=<X> (theory 0.5),
    linear b=<Y> (theory 1.0).

E6. Proofread  (R4-3). Confirmed defects:
    Section I    "primarily optimize encrypted retrieval encrypted retrieval"
                 -> delete the duplicate
    Section III-C "To ease of understanding, Table Table~\ref{tab:notation}"
                 -> "For ease of understanding, Table~\ref{tab:notation}"
    Section V    prose says "Table IV" while the label resolves to
                 tab:complexity_comparison -- check every hard-coded number
    The \DeclareUnicodeCharacter directives at the top strip LTR/RTL marks,
    a sign invisible characters were pasted in. Re-scan before submitting.


================================================================================
  PART F -- DECISIONS REQUIRED FROM THE AUTHORS
================================================================================

F1. Trinity baseline. It currently CRASHES on index construction:
      trinity.py:210 shve.encrypt ->
      struct.error: 'I' format requires 0 <= number <= 4294967295
    Pre-existing, unrelated to the rebuild. Likely normalize_coordinate on
    the timestamp, since Datarecord.csv begins exactly at the 2024-01-01
    window boundary Trinity assumes. Fix it, or drop Trinity as a baseline
    and remove it from Tables I, IV and V.

F2. Revocation (B9): declare the limitation, or make node keys epoch-bound
    and re-measure index maintenance.

F3. Raspberry Pi for Experiment 9: keep one, or drop the heterogeneous
    hardware claim entirely. (Energy is already resolved -- see C4.)

F4. Multi-node blockchain for Experiment 11: stand up 3+ nodes, or state
    that consortium-scale behaviour was not evaluated.


================================================================================
  PART G -- NOT A PAPER EDIT, BUT DO IT
================================================================================

G1. ROTATE THE MONGODB CREDENTIALS. A live Atlas connection string with
    username and password was committed in benchmark_paper.py and the ECGRQ
    config.py. Removing the files does not remove them from git history.

G2. Fix user_client.py line 21 (self.Ks = secrets["Ks"]) -- see B3.

G3. blockchain_edge.py has NO multi-proof implementation; it attaches only
    per-node pi_u, and user_client's multi_proof branch is dead code.
    Table IV claims O(r log(N/r)) verification, but the production path is
    O(r log N). Either implement multi-proof generation, or correct Table IV.

================================================================================
  END
================================================================================
